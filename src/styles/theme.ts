const color = {
  black: '#000',
  white: '#fff',
  header_bg: '#11101d',
};

const size = {
  header_active: '240px',
  header: '78px',
};

const theme = {
  color,
  size,
};

export default theme;
